<ul id="users-list">
	<% @users.each do |user| %>
	<li>
		<span id="<%= user.id %>-status" class="<%= user.online ? 'online' : 'offline' %>"></span>
		<%= link_to user.username, conversations_path(user_id: user), remote: true, method: :post %>
	</li>
	<% end %>
</ul>