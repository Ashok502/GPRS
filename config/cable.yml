development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  url:     ENV["REDIS_URL"]
  host: ENV["RHOST"]
  port: ENV["RPORT"]
  password: ENV["RPWD"]
  inline: true
  timeout: 1
